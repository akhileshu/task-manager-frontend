import React from "react";
import "../cssFiles/loader.css"; // Import the CSS file

function Loader() {
  return (
    <div className="loader-wrapper">
      <div className="loader"></div>
    </div>
  ); // Apply the CSS class
}

export default Loader;
